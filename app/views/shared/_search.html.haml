= form_tag articles_path, method: :get, class: 'mui-form' do
  .mui-textfield
    = text_field_tag( :search, '', { class: '',
      placeholder: "Search by title, or language, e.g. Ruby",
      type: "text"})
    .control
      = submit_tag('Search', {class: "mui-btn mui-btn--primary"})
