<h1>Editing Section</h1>

<%= render 'form', section: @section %>

<%= link_to 'Show', article_section_path(@section.article, @section) %> |
<%= link_to 'Back', article_sections_path(@section.article) %>
